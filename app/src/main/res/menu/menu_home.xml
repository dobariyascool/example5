<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context="com.arraybit.pos.GuestHomeActivity">

    <item
        android:id="@+id/login"
        android:icon="@mipmap/login"
        android:orderInCategory="200"
        android:title="@string/navLogin"
        app:showAsAction="never"/>

    <item
        android:id="@+id/registration"
        android:icon="@mipmap/registration"
        android:orderInCategory="300"
        android:title="@string/navRegistration"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="@string/navSearch"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"/>

</menu>
